# numbers = [1, 2, 3, 4]
# doubled = list(map(lambda x: x * 2, numbers))
# even_numbers = list(filter(lambda x: x % 2 == 0, numbers))
# print(doubled,even_numbers)

# names = ['Alice', 'Bob', 'Charlie']
# ages = [25, 30, 35]
# for name, age in zip(names, ages):
#     print(name, age)

num = 2
# num = num * 10 
num = num%10
print(num)